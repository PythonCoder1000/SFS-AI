# Game disassembly dumps -- not our code, don't publish
scratch/

# Generated telemetry / flight logs
sfsprobe/archive/
*.jsonl

# Build artifacts (regenerate via sfsprobe/build.sh)
*.dll

# Python virtual environments and caches (regenerate via
# `pip install -r requirements.txt` in each service's own venv)
venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Local working-procedure notes -- not for the repo
CLAUDE.md
python/prediction_demo_data.json
temp_claude_state/
